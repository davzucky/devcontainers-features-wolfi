{
    "default": {
        "image": "chainguard/wolfi-base:latest",
        "initializeCommand": {
            "mkdir-posix": "mkdir -p ${TEMP:-/tmp}/opencode || true"
        },
        "features": {
            "bash": {},
            "opencode": {}
        }
    },
    "with_auth": {
        "image": "chainguard/wolfi-base:latest",
        "initializeCommand": {
            "prepare-posix": "mkdir -p ${TEMP:-/tmp}/opencode && printf '{\"token\":\"test\"}' > ${TEMP:-/tmp}/opencode/auth.json || true"
        },
        "features": {
            "bash": {},
            "opencode": {
                "copyAuth": true
            }
        }
    }
}
