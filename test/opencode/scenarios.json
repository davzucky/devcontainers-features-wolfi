{
    "default": {
        "image": "chainguard/wolfi-base:latest",
        "initializeCommand": {
            "mkdir-posix": "mkdir -p $HOME/.local/share/opencode || true"
        },
        "features": {
            "bash": {},
            "opencode": {}
        }
    },
    "with_auth": {
        "image": "chainguard/wolfi-base:latest",
        "initializeCommand": {
            "mkdir-posix": "mkdir -p $HOME/.local/share/opencode || true"
        },
        "features": {
            "bash": {},
            "opencode": {
                "copyAuth": true
            }
        }
    }
}
